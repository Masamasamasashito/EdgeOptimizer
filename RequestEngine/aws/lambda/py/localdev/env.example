# ------------------------------------------------------------------------------
# Important!! Before Settings
# ------------------------------------------------------------------------------
# To do 1st
# cp env.example .env

# ------------------------------------------------------------------------------
# 1. LAMBDA Layer Settings ( only docker-compose.yml )
# ------------------------------------------------------------------------------
# 将来的に環境変数化する場合は、以下のような設定を追加できます：
# LAMBDA_LAYER_VOLUME_NAME=eoLambdaLayerDataVolume

# ------------------------------------------------------------------------------
# 2. COMMON_LAMBDA_LAYER Settings ( docker-compose.yml ＆ Dockerfile )
# ------------------------------------------------------------------------------
# .env 環境変数の受け渡し（docker-compose.yml と Dockerfile）
# 1. .env 環境変数 COMMON_LAMBDA_LAYER_XXX_YYY
# 2. → docker-compose.yml の ${COMMON_LAMBDA_LAYER_XXX_YYY:-sample_value}
# 3. → docker-compose.yml で Dockerfile の ARG 変数名に変換（COMMON_プレフィックスを削除）
#    例: COMMON_LAMBDA_LAYER_XXX_YYY → LAMBDA_LAYER_XXX_YYY
# 4. → Dockerfile では ARG 変換後の名前（例: ARG LAMBDA_LAYER_XXX_YYY → ${LAMBDA_LAYER_XXX_YYY} として使用）
#    docker-compose.ymlから値が渡される場合、その値が使用される
#    値が渡されない場合は、Dockerfileで指定しているデフォルト値が使用される

# Docker Official Image
COMMON_LAMBDA_LAYER_DOCKER_IMAGE=python
COMMON_LAMBDA_LAYER_DOCKER_IMAGE_TAG=slim

# ------------------------------------------------------------------------------
# 3. Network Binding
# ------------------------------------------------------------------------------
# Default to 127.0.0.1 to avoid exposing ports publicly and to prevent conflicts
# 現在、Lambda Layer Builderはポートを公開していないため、この設定は不要
# DOCKER_HOST_BIND_ADDR=127.0.0.1
