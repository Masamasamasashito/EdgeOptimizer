# RequestEngine Instance Definition
# Copy this file and rename to {instance_type}{instance_id}.env
# Example: lambda001.env, lambda002.env
#
# Referenced by (この定義値を使用するファイル):
#   ../../../../../.github/workflows/deploy-py-to-aws-lambda.yml
#   ../CFn/re-aws-cfnstack.yml (パラメータ化済み)
EoServiceServerless=lambda
EoNanoidSlug=001
EoArea=ap-northeast-1
EoAreaShort=apne1
