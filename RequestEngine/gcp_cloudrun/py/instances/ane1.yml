# Referenced by (この定義値を使用するファイル):
#   ../../../../.github/workflows/deploy-py-to-gcp-cloudrun.yml (EO_GCP_CLOUD_RUN_SERVICE_NAME, EO_GCP_REGION)
#   ../terraform/cloud_run.tf (locals から構築)
#   ../terraform/main.tf (name_prefix + region_short)
instance_id: "ane1"
resource_name: "eo-re-d01-cloudrun-ane1"
region: "asia-northeast1"
