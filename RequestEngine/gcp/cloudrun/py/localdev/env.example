# ------------------------------------------------------------------------------
# Important!! Before Settings
# ------------------------------------------------------------------------------
# To do 1st
# cp env.example .env

# ------------------------------------------------------------------------------
# 1. GCP Cloud Run Settings ( only docker-compose.yml )
# ------------------------------------------------------------------------------
# 将来的に環境変数化する場合は、以下のような設定を追加できます：
# GCP_CLOUDRUN_VOLUME_NAME=eoGcpCloudrunDataVolume

# ------------------------------------------------------------------------------
# 2. COMMON_GCP_CLOUDRUN Settings ( docker-compose.yml ＆ Dockerfile )
# ------------------------------------------------------------------------------
# .env 環境変数の受け渡し（docker-compose.yml と Dockerfile）
# 1. .env 環境変数 COMMON_GCP_CLOUDRUN_XXX_YYY
# 2. → docker-compose.yml の ${COMMON_GCP_CLOUDRUN_XXX_YYY:-sample_value}
# 3. → docker-compose.yml で Dockerfile の ARG 変数名に変換（COMMON_プレフィックスを削除）
#    例: COMMON_GCP_CLOUDRUN_XXX_YYY → GCP_CLOUDRUN_XXX_YYY
# 4. → Dockerfile では ARG 変換後の名前（例: ARG GCP_CLOUDRUN_XXX_YYY → ${GCP_CLOUDRUN_XXX_YYY} として使用）
#    docker-compose.ymlから値が渡される場合、その値が使用される
#    値が渡されない場合は、Dockerfileで指定しているデフォルト値が使用される

# Docker Official Image
# Python 3.14 2026/1/16
COMMON_GCP_CLOUDRUN_DOCKER_IMAGE=python
COMMON_GCP_CLOUDRUN_DOCKER_IMAGE_TAG=slim

# ------------------------------------------------------------------------------
# 3. Network Binding
# ------------------------------------------------------------------------------
# Default to 127.0.0.1 to avoid exposing ports publicly and to prevent conflicts
DOCKER_HOST_BIND_ADDR=127.0.0.1

# ------------------------------------------------------------------------------
# 4. GCP Cloud Run Application Settings
# ------------------------------------------------------------------------------
# Cloud Run アプリケーション用の環境変数
# EO_CLOUDRUN_REQUEST_SECRET_LOCAL: ローカル開発用の照合用リクエストシークレット（EO_Infra_Docker/.envのN8N_EO_REQUEST_SECRETと同じ値）
EO_CLOUDRUN_REQUEST_SECRET_LOCAL=your-secret-here
EO_GCP_PROJECT_ID=
# CLOUDRUN_REQUEST_SECRET_NAME: GCP Secret Managerのシークレット名（EO_Documents/Manuals/RE_README.mdを参照）
CLOUDRUN_REQUEST_SECRET_NAME=eo-re-d1-secretmng

