---
description: プロジェクト共通のルール・慣習（Git ほか）
globs: 
alwaysApply: true
---

# プロジェクト共通ルール

## Git

- **git commit を実行しないこと。** ユーザーが明示的に「コミットして」と依頼した場合を除き、`git commit` や `git add` のうえで `git commit` するコマンドをターミナルで実行してはならない。
- コードの変更・編集の提案までにとどめ、コミットはユーザーが手動で行う。
