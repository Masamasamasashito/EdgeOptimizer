# ==============================================================================
# EO Terraform Docker - 環境変数例
# ==============================================================================
# 使い方:
#   1. このファイルを .env にコピー: cp env.example .env
#   2. 必要に応じて値を編集
#   3. 機密（TF_VAR_req_secret）は .env にのみ記載し、Git にコミットしない
# ==============================================================================
# Terraform
# https://hub.docker.com/r/hashicorp/terraform
# ------------------------------------------------------------------------------
# 共通
# ------------------------------------------------------------------------------
COMMON_TIMEZONE=Asia/Tokyo

# Terraform のベースイメージ（docker-compose の build args に渡す）
COMMON_EO_DOCKER_TERRAFORM_IMAGE=hashicorp/terraform
COMMON_EO_DOCKER_TERRAFORM_IMAGE_TAG=latest

# ------------------------------------------------------------------------------
# ワークスペース
# ------------------------------------------------------------------------------
# Terraform コードがあるディレクトリ（docker-compose 基準の相対パスまたは絶対パス）
# リポジトリルートの terraform/ を使う場合はそのまま
EO_TERRAFORM_WORKSPACE=../terraform

# ------------------------------------------------------------------------------
# クラウド認証パス（docker-compose.override.yml でマウントする場合に設定）
# ------------------------------------------------------------------------------
# 使用するクラウドの認証ディレクトリをフルパスで指定。
# Linux/Mac 例: /home/user/.aws
# Windows 例: C:\Users\YourName\.aws または %USERPROFILE%\.aws
# 未設定の場合は docker-compose.override.yml でボリュームを追加しないこと
# EO_TF_AWS_CREDENTIALS_PATH=
# EO_TF_AZURE_CREDENTIALS_PATH=
# EO_TF_GCP_CREDENTIALS_PATH=

# ------------------------------------------------------------------------------
# Terraform 変数（機密）
# ------------------------------------------------------------------------------
# Request Engine 用シークレット。terraform.tfvars に書かず、ここで渡すことを推奨
# EO_TF_VAR_req_secret=
